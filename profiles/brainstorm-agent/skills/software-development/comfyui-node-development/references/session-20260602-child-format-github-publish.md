# 2026-06-02 Session: PromptLibraryNode Child Format Fix + GitHub Publish + Code Merging

## 变更摘要

### PromptLibraryNode V20.5 → V20.6（隐式版本）

1. **角色特征维度精确化** — 14处system prompt角色特征定义统一修改
2. **儿童视频格式二重写** — 从无格式定义 → 完整的起承转合四幕+8维度格式
3. **儿童微动视频/GIF重写** — 从4行提示 → 完整格式定义(核心动作/画面/动效)
4. **儿童绘本格式重写** — 从半残 → 完整格式(画面/文案/旁白对话/构图景别)
5. **总纲符号清理** — 4处硬编码的 `1、` `2、` `【{mode}总纲】` → 纯文字无符号
6. **输出约束加强** — 所有模式末尾加统一输出约束禁止符号标记
7. **GitHub发布** — 节点发布到独立GitHub仓库

### Hermes 增强包整合

1. **代码合并** — 21→4统一模块
2. **底层注入** — PRE/POST钩子注入 `conversation_loop.py` + `run_agent.py`
3. **gear_enforcer修复** — 硬编码路径改为 `Path.home()`
4. **全量备份** — 171MB

## 关键接口

角色的特征定义（所有14处）:
```
角色特征：仅在角色外貌/服装/状态有实质性变化时输出（换装/变脏/受伤/新增饰品等可见变化）。禁止写角色动作叙事（那是画面维度的事）。无变化时此行整行不出现。
```

输出约束（所有模式末尾）:
```
输出约束：严禁使用任何符号标记——禁止#、禁止**、禁止-开头、禁止→、禁止1. 2. 3.编号、禁止---分隔线。直接用纯文字叙述。各维度之间用换行分隔，不要用符号装饰维度名称。
```

## 格林主人风格偏好（本轮新增）

- 说人话，去符号。README禁止使用 ## ** - 1. --- 等
- 风险警告用大红字32px居中
- commit message极简，如 "README"
- git author名不能是 "Hermes Enhancement"
- 执行任务时不要停下汇报，直接继续